from typing import List, Tuple


async def summarize_text(text: str) -> Tuple[str, List[str]]:
    """Return a placeholder summary and example tags.

    This stub stands in for a future AI-powered summarization call. Replace the
    fixed values with an invocation to your model or API when you are ready to
    integrate real summarization.
    """

    summary = "This is a placeholder summary of the uploaded legal document."
    tags = ["placeholder", "draft", "summary"]
    return summary, tags
